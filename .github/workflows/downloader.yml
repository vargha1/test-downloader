name: Download release and commit (LFS)
on:
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  commit-release-asset:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Install Git LFS
      run: |
        sudo apt-get update
        sudo apt-get install -y git-lfs
    - name: Configure LFS
      run: |
        git lfs install --local
        git lfs track "cf-scanner_Linux_arm64.tar.gz"
        git add .gitattributes || true
    - name: Download latest release asset
      uses: robinraju/release-downloader@v1
      with:
        repository: radioactiveAHM/cf-scanner
        latest: true
        fileName: cf-scanner_Linux_arm64.tar.gz
        out-file-path: ${{ github.workspace }}
    - name: Commit and push
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add cf-scanner_Linux_arm64.tar.gz .gitattributes
        git commit -m "Add cf-scanner_Linux_arm64.tar.gz (latest release)" || echo "No changes"
        git push
      env:
        GIT_LFS_SKIP_SMUDGE: "0"
